#!/bin/sh

case "$(printf "󰌾 lock\n󰗽 leave Hyprland\n󰁯 reboot\n󰐥 shutdown" | wofi -d 'Action: ')" in
	'󰌾 lock') swaylock ;;
	'󰗽 leave Hyprland') kill -TERM "$(pgrep -u "$USER" "\bHyprland$")" ;;
	'󰁯 reboot') systemctl reboot -i ;;
	'󰐥 shutdown') systemctl poweroff -i ;;
	*) exit 1 ;;
esac
